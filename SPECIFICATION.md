# 📜 ТЗ: Персональный ассистент разработчика (AI Assistant Hub)

## 1. Общие требования к системе

### Архитектура

- **Сервер (FastAPI + SQLite)** — **домашний ПК**. Более доступен со всех устройств.
- **Ollama (RTX 3050, 8 ГБ)** — **домашний ПК**.
- **Веб-интерфейс** (телефон) — **домашний ПК**.
- **GUI (CustomTkinter)** — **ноутбук** (клиент).
- **Сканер проектов** — **ноутбук** (видит проекты локально).
- **Git-трекер** — **ноутбук** (видит репозитории локально).
- **Связь** — Tailscale.

### Правила работы

- Телефон **или** ноутбук — **никогда** одновременно. Нет конкуренции за БД.
- Ноутбук без домашнего ПК — **нормально**. Ассистент недоступен, код — работает.
- Бэкап БД — через Git (дамп в SQL/JSON).

### Клиенты

- **Ноутбук:** CustomTkinter — полный доступ (задачи, чат, сканер, Git, отчёты).
- **Телефон:** веб-интерфейс — задачи, идеи, отчёты, оценки.

---

## 🛠️ ПОШАГОВЫЙ ПЛАН РАЗРАБОТКИ

### 📁 ЭТАП 1: Серверное ядро БД (`core/database.py`) ✅

- **1.1** — `sqlite3` + `get_db_connection` с `PRAGMA foreign_keys = ON`. ✅
- **1.2** — таблица `projects`. ✅
- **1.3** — таблица `tasks` с `CHECK` на `task_type` и `status`. ✅
- **1.4** — `add_project`, `add_task` с обработкой `IntegrityError`. ✅
- **1.5** — `delete_project`, `delete_task` с `rowcount`. ✅
- **1.6** — таблица `completed_tasks_log`. ✅

---

### 📁 ЭТАП 2: Умный сканер (`desktop_ui/scanner.py`) 👈 следующий

> Сканер работает **на ноутбуке**, где лежат проекты.

- **2.1** — функция поиска и чтения `.gitignore` в папке проекта.
- **2.2** — компиляция правил игнорирования через `pathspec`.
- **2.3** — ленивый рекурсивный обход через `os.walk` с фильтрацией мусора (`.venv`, `__pycache__`, `.git`).
- **2.4** — форматирование в текстовое дерево со значками 📁 и 📄.
- **2.5** — отправка результата на сервер через HTTP (если доступен) или кэш.

---

### 📁 ЭТАП 3: Мобильный веб-мост (`core/mobile_bridge.py`)

> Работает **на домашнем ПК**. Доступен с телефона всегда, пока ПК включён.

- **3.1** — каркас FastAPI для приёма заметок с телефона.
- **3.2** — эндпоинт добавления идеи с привязкой к проекту.
- **3.3** — сохранение идеи в БД со статусом `backlog`.
- **3.4** — HTML-страница для ввода (мобильная адаптация).
- **3.5** — базовая HTTP-аутентификация.

---

### 📁 ЭТАП 4: Генератор проектов (`core/project_creator.py`)

- **4.1** — шаблоны (`.gitignore`, `ruff.toml`, `.vscode/settings.json`).
- **4.2** — `subprocess` для `git init` и `poetry new`.
- **4.3** — запись файлов + `README.md`.
- **4.4** — шаблоны типовых задач («настроить окружение», «README», «первый коммит»).

---

### 📁 ЭТАП 5: Git-трекер и дисциплина (`desktop_ui/task_manager.py`)

> Работает **на ноутбуке**. Результат → на сервер для блокировки задач.

- **5.1** — `git status --porcelain` через `subprocess`.
- **5.2** — `git branch --show-current`.
- **5.3** — блокировка закрытия задачи при незакоммиченных изменениях.
- **5.4** — генерация отчёта за смену по закрытым за день задачам (таблица `completed_tasks_log`).

---

### 📁 ЭТАП 6: Async-клиент Ollama (`core/client_ollama.py`)

> Работает **на домашнем ПК**. Ноутбук и телефон ходят через Tailscale.

- **6.1** — `httpx.AsyncClient` с таймаутами.
- **6.2** — retry + backoff для мобильного интернета.
- **6.3** — healthcheck домашнего сервера.
- **6.4** — логика выбора модели:
  - `qwen2.5-coder:7b` — вопросы по коду;
  - `qwen2.5:7b` — рефлексия и оценки.
- **6.5** — обработка ошибок (`ConnectError`, `TimeoutException`) с понятными сообщениями.

---

### 📁 ЭТАП 7: Сессии и чат (`core/session_manager.py`)

- **7.1** — таблица `sessions` (`id`, `project_id`, `started_at`, `ended_at`).
- **7.2** — таблица `chat_messages` (`id`, `session_id`, `role`, `content`, `created_at`).
- **7.3** — функция начала сессии.
- **7.4** — функция окончания сессии.
- **7.5** — функция записи сообщения в чат.
- **7.6** — функция получения чата за сессию.

---

### 📁 ЭТАП 8: Рефлексия и оценка знаний (`core/reflection.py`)

> Работает **на домашнем ПК**. Доступно с телефона.

#### 8.1: Система оценки

- **10 тем:** HTTP, SQL, async, Git, тесты, типизация, алгоритмы, ООП, безопасность, архитектура.
- **Шкала 1–10** с описанием уровней.
- **Минимум 3 сообщения** по теме — иначе тема пропускается.
- **20 последних оценок по теме** — скользящее окно (не 20 сессий).
- **Простое среднее** для сравнения.
- Таблица `topic_scores` (`session_id`, `topic`, `score`, `date`).

#### 8.2: Классификация

- **Фоном**, молча, после ответа LLM.
- **На лету — ничего не показывается.**
- Вход — **только твоё сообщение**.
- Выход — тема + кандидат в ошибку.

#### 8.3: Тактический отчёт (после сессии)

- **Вход:** классифицированные сообщения за сессию.
- **LLM-промпт:** «строгий учитель».
- **Выход:**
  - оценки по темам (где ≥3 сообщения),
  - сравнение с 20 последними оценками по теме,
  - причина изменения (если оценка изменилась),
  - ошибки + паттерны + «обрати внимание».
- **Развернуть** любую оценку → причина + сообщения.
- **Кнопка «Принять»** → оценки сохраняются в БД.
- **Оспаривание** → «не ошибка» / «контекст» → не идёт в следующий отчёт.

#### 8.4: Стратегический отчёт (раз в проект)

- **Вход:** сводка по всем сессиям проекта.
- **Выход:** тренды по темам + паттерны.
- **Формат:** Markdown.

#### 8.5: Хранение

- **Тактические** — в БД + Markdown.
- **Стратегические** — Markdown.
- **Оценки** — в БД (только принятые).

---

### 📁 ЭТАП 9: Графический интерфейс (`desktop_ui/app.py`)

- **9.1** — главное окно (дашборд / список задач / чат).
- **9.2** — двухслойный список задач.
- **9.3** — дашборд статистики («проектов в работе», «идей в бэклоге»).
- **9.4** — всплывающие предупреждения Git.
- **9.5** — кнопки «Начать сессию» / «Закончить сессию».
- **9.6** — индикатор статуса сервера (healthcheck).
- **9.7** — просмотр тактических отчётов.
- **9.8** — просмотр стратегических отчётов.
- **9.9** — экран оценок по 10 темам (со сравнением за 20).

---

### 📁 ЭТАП 10: Финал

- **10.1** — Docker (по желанию, для упаковки).
- **10.2** — README.md с помощью ИИ + правка.
- **10.3** — тесты ключевых модулей (после стабилизации API).
- **10.4** — бэкап БД через Git (дамп в SQL/JSON).

---

## 📋 Сводка изменений от первой версии ТЗ

| Что | Было | Стало |
|---|---|---|
| Сервер | Домашний ПК | Домашний ПК (без изменений) |
| Сканер | На сервере | **На ноутбуке** |
| Git-трекер | На сервере | **На ноутбуке** |
| Telegram | Везде | **Убран** |
| Мобильный доступ | Telegram | **Веб** |
| Этап 5.4 | Перенесён | Возвращён на место |
| Рефлексия | Не было | **Полная система** (Этап 8) |
| Показ на лету | Планировался | **Убран** |
| Батчи | Планировались | **Убраны** |
| Оценки | Не было | 10 тем, шкала 1–10, окно 20 |
| Кнопка «Принять» | Не было | **Есть** |
| Бэкап | Не было | **Git (дамп БД)** |