# ТЗ: Персональный ассистент разработчика (AI Assistant Hub)

## 1. Общие требования к системе
* **Платформа:** Linux (ноутбук разработчика) + удаленный ПК (сервер с Ollama через Tailscale).
* **Язык и среда:** Python 3.12, управление зависимостями через Poetry, код-стайл контролируется Ruff.
* **Хранение данных:** Локальная СУБД SQLite на ноутбуке для централизованного контроля всех проектов.
* **Интерфейс:** Десктопное окно на CustomTkinter (Тёмная тема по умолчанию) + мобильный доступ для фиксации идей.

---

## 🛠️ Пошаговый план разработки (Backlog задач)

### 📁 ЭТАП 1: Ядро базы данных (`core/database.py`)
* **Задача 1.1:** Подключить встроенный модуль `sqlite3` и настроить создание файла базы данных `assistant.db` в корне приложения.
* **Задача 1.2:** Спроектировать и написать SQL-запрос для таблицы `projects` (хранит ID, имя проекта, путь на диске, дату создания).
* **Задача 1.3:** Спроектировать таблицу `tasks` для двухслойного списка (ID, `project_id`, название, тип: "big_block" или "subtask", `parent_id` для связи подзадачи с большим блоком, статус: "todo"/"done"/"backlog").
* **Задача 1.4:** Написать базовые Python-функции (CRUD) для добавления нового проекта и новой задачи в базу данных.

### 📁 ЭТАП 2: Умный сканер проекта (`core/scanner.py`)
* **Задача 2.1:** Написать функцию поиска и чтения файла `.gitignore` в выбранной папке проекта.
* **Задача 2.2:** Настроить компиляцию правил игнорирования через внешнюю библиотеку `pathspec`.
* **Задача 2.3:** Реализовать ленивый рекурсивный обход папок через `os.walk` с жесткой фильтрацией системного мусора (`.venv`, `__pycache__`, `.git`).
* **Задача 2.4:** Написать форматирование результата в красивое текстовое дерево со значками 📁 и 📄 для отправки в ИИ.

### 📁 ЭТАП 3: Мобильный мост для идей (`core/mobile_bridge.py`)
* **Задача 3.1:** Написать каркас для приема сообщений (выбираем легкий Telegram-бот на `telebot` или `aiogram`, либо FastAPI-сервер).
* **Задача 3.2:** Реализовать команду/эндпоинт добавления идеи с привязкой к конкретному проекту.
* **Задача 3.3:** Написать сохранение мобильной идеи в базу данных со статусом `backlog`.

### 📁 ЭТАП 4: Генератор новых проектов (`core/project_creator.py`)
* **Задача 4.1:** Написать шаблоны дефолтных файлов для генерации (`.gitignore`, `ruff.toml`, `.vscode/settings.json` с автозапуском окружения).
* **Задача 4.2:** Использовать модуль `subprocess` для автоматического вызова команд `git init` и `poetry new` внутри создаваемой папки.
* **Задача 4.3:** Реализовать функцию записи файлов-настроек и шаблона `README.md` в новую папку.

### 📁 ЭТАП 5: Git-трекер и Менеджер дисциплины (`core/task_manager.py`)
* **Задача 5.1:** Написать функцию вызова команды `git status --porcelain` через `subprocess` для проверки чистоты рабочей директории.
* **Задача 5.2:** Написать функцию проверки текущей ветки через `git branch --show-current`, чтобы запретить работу в `main`.
* **Задача 5.3:** Реализовать логику валидации: блокировать перевод чекбокса подзадачи в статус `done`, если в Git есть незакоммиченные изменения.
* **Задача 5.4:** Написать функцию автоматической генерации отчета за смену по закрытым за день задачам.

### 📁 ЭТАП 6: Асинхронный сетевой клиент (`core/client_ollama.py`)
* **Задача 6.1:** Настроить асинхронный клиент `httpx.AsyncClient` для отправки JSON-пакетов без зависания интерфейса.
* **Задача 6.2:** Прописать жесткую логику переключения моделей: отправка вопросов по коду на `qwen2.5-coder:7b` (`temperature: 0.0`), а генерацию отчетов на `llama3.1:8b`.

### 📁 ЭТАП 7: Графический интерфейс (`desktop_ui/app.py`)
* **Задача 7.1:** Создать главное окно приложения на CustomTkinter с разделением на зону дашборда, список задач и окно чата.
* **Задача 7.2:** Реализовать динамический вывод двухслойного списка задач (Заголовки блоков + смещенные чекбоксы подзадач), завязанный на базу данных.
* **Задача 7.3:** Вывести дашборд статистики («Столько-то проектов в работе, столько-то идей в бэклоге»).
* **Задача 7.4:** Интегрировать всплывающие предупреждения при попытке нарушить правила Git.
