# 📜 ТЗ: Персональный ассистент разработчика (AI Assistant Hub)

## 1. Общие требования к системе

### Архитектура

- **Сервер (FastAPI + SQLite)** — **домашний ПК**. Более доступен со всех устройств.
- **Ollama (RTX 3050, 8 ГБ)** — **домашний ПК**.
- **Веб-интерфейс** (телефон) — **домашний ПК**.
- **GUI (CustomTkinter)** — **ноутбук** (клиент).
- **Сканер проектов** — **ноутбук** (видит проекты локально).
- **Git-трекер** — **ноутбук** (видит репозитории локально).
- **Связь** — Tailscale.

### Правила работы

- Телефон **или** ноутбук — **никогда** одновременно. Нет конкуренции за БД.
- Ноутбук без домашнего ПК — **нормально**. Ассистент недоступен, код — работает.
- Бэкап БД — через Git (дамп в SQL/JSON).
- Внешнее включение ПК: `pc_1` / `pcoff_1`.

### Клиенты

- **Ноутбук:** CustomTkinter — полный доступ.
- **Телефон:** веб-интерфейс — идеи, отчёты, оценки, чат.

---

## 🛠️ ПОШАГОВЫЙ ПЛАН РАЗРАБОТКИ

### 📁 ЭТАП 1: Серверное ядро БД (`core/database.py`) ✅

- **1.1** — `sqlite3` + `get_db_connection` с `PRAGMA foreign_keys = ON`. ✅
- **1.2** — таблица `projects`. ✅
- **1.3** — таблица `tasks` с `CHECK`. ✅
- **1.4** — `add_project`, `add_task`. ✅
- **1.5** — `delete_project`, `delete_task` с `rowcount`. ✅
- **1.6** — таблица `completed_tasks_log`. ✅
- **1.7** — таблица `project_context` (ТЗ, описание). 🆕
- **1.8** — таблица `ideas` (идеи проекта). 🆕

---

### 📁 ЭТАП 2: Умный сканер (`desktop_ui/scanner.py`) ✅

- **2.1** — `load_gitignore_rules`. ✅
- **2.2** — `collect_all_ignores`, `compile_ignore_spec`. ✅
- **2.3** — `build_project_tree` (`os.walk` с фильтрацией). ✅
- **2.4** — `format_tree_to_string` (дерево со значками). ✅

---

### 📁 ЭТАП 3: Мобильный веб-мост (`core/mobile_bridge.py`) 👈 следующий

> Только форма идей. Без чата.

- **3.1** — каркас FastAPI для приёма заметок с телефона.
- **3.2** — эндпоинт добавления идеи с привязкой к проекту.
- **3.3** — сохранение идеи в таблицу `ideas`.
- **3.4** — HTML-страница для ввода (мобильная адаптация).
- **3.5** — базовая HTTP-аутентификация.

---

### 📁 ЭТАП 4: Генератор проектов (`core/project_creator.py`)

- **4.1** — шаблоны (`.gitignore`, `ruff.toml`, `.vscode/settings.json`).
- **4.2** — `subprocess` для `git init` и `poetry new`.
- **4.3** — запись файлов + `README.md`.
- **4.4** — шаблоны типовых задач.

---

### 📁 ЭТАП 5: Git-трекер и дисциплина (`desktop_ui/task_manager.py`)

- **5.1** — `git status --porcelain`.
- **5.2** — `git branch --show-current`.
- **5.3** — блокировка закрытия задачи при незакоммиченных изменениях.
- **5.4** — отчёт за смену по `completed_tasks_log`.

---

### 📁 ЭТАП 6: Async-клиент Ollama (`core/client_ollama.py`)

- **6.1** — `httpx.AsyncClient` с таймаутами.
- **6.2** — retry + backoff.
- **6.3** — healthcheck домашнего ПК.
- **6.4** — выбор модели: `qwen2.5-coder:7b` (код), `qwen2.5:7b` (рефлексия).
- **6.5** — обработка ошибок.

---

### 📁 ЭТАП 7: Сессии и чат (`core/session_manager.py`)

- **7.1** — таблица `sessions`.
- **7.2** — таблица `chat_messages`.
- **7.3** — функция начала сессии (ручная кнопка).
- **7.4** — функция окончания сессии (ручная или автозакрытие при выходе из GUI).
- **7.5** — функция записи сообщения.
- **7.6** — функция получения чата за сессию.
- **7.7** — при старте GUI: автозакрытие старой открытой сессии.

---

### 📁 ЭТАП 8: Рефлексия и оценка знаний (`core/reflection.py`)

#### 8.1: Система оценки
- 10 тем: HTTP, SQL, async, Git, тесты, типизация, алгоритмы, ООП, безопасность, архитектура.
- Шкала 1–10.
- Минимум 3 сообщения по теме.
- 20 последних оценок по теме.
- Простое среднее.
- Таблица `topic_scores`.

#### 8.2: Классификация
- Фоном, молча. На лету — ничего не показывается.
- Вход — твоё сообщение.
- Выход — тема + кандидат в ошибку.

#### 8.3: Тактический отчёт
- По кнопке «Закончить сессию».
- Оценки по темам, сравнение с 20, причина изменения.
- Развернуть любую оценку.
- Кнопка «Принять» + оспаривание.

#### 8.4: Стратегический отчёт (раз в проект)
- Тренды + паттерны.
- Markdown.

#### 8.5: Хранение
- Тактические — БД + Markdown.
- Стратегические — Markdown.
- Оценки — только принятые.

---

### 📁 ЭТАП 9: Графический интерфейс (`desktop_ui/app.py`)

- **9.1** — главное окно (дашборд / задачи / чат).
- **9.2** — двухслойный список задач.
- **9.3** — дашборд статистики.
- **9.4** — предупреждения Git.
- **9.5** — кнопки «Начать сессию» / «Закончить сессию».
- **9.6** — индикатор статуса сервера (healthcheck).
- **9.7** — просмотр тактических отчётов.
- **9.8** — просмотр стратегических отчётов.
- **9.9** — экран оценок по 10 темам.

---

### 📁 ЭТАП 10: Веб-чат с контекстом (`core/web_chat.py`) 🆕

> Расширение веб-моста. Доступно с телефона.

- **10.1** — эндпоинт чата с привязкой к проекту.
- **10.2** — сборка контекста для ИИ: ТЗ + описание.
- **10.3** — первое сообщение сессии: ТЗ + сводка последних отчётов.
- **10.4** — последующие сообщения: только ТЗ.
- **10.5** — суммирование ТЗ, если не влезает в контекст модели.
- **10.6** — HTML-страница чата (мобильная адаптация).
- **10.7** — сохранение диалога в `chat_messages` (как в GUI).

---

### 📁 ЭТАП 11: Финал

- **11.1** — Docker (по желанию).
- **11.2** — README.md с помощью ИИ.
- **11.3** — тесты ключевых модулей.
- **11.4** — бэкап БД через Git.

---

## 📋 Отдельно: BACKLOG.md

- Файл в корне проекта.
- Туда — идеи, которые приходят вечером.
- Раз в неделю — просмотр. Что-то переходит в ТЗ, что-то удаляется.
- **Не** часть ТЗ.